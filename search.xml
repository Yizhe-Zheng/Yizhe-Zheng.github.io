<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World</title>
    <url>/2021/04/20/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<span id="more"></span>

<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
      <tags>
        <tag>-Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>Github&amp;hexo创建个人静态网站&amp;next主题优化配置</title>
    <url>/2021/04/20/Github&amp;hexo%E5%88%9B%E5%BB%BA%E4%B8%AA%E4%BA%BA%E9%9D%99%E6%80%81%E7%BD%91%E7%AB%99&amp;next%E4%B8%BB%E9%A2%98%E4%BC%98%E5%8C%96%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<p>这篇文章参考了许多博客和教程的内容，主要是给自己做这个网页做个记录，以防止以后有问题再去搜索浪费时间，后续学到其他的美化功能应该都会添加到这篇文章里，当然也有可能为了增加文章数再开一篇新文章 ┗( ▔, ▔ )┛,参考的博文和教程都放在文章最后面，有问题可以回原文去看看。</p>
<span id="more"></span>

<h1 id="注册一个-Github-账号"><a href="#注册一个-Github-账号" class="headerlink" title="注册一个 Github 账号"></a>注册一个 Github 账号</h1><p>自行使用邮箱注册即可。</p>
<hr>
<h1 id="安装设置-Git"><a href="#安装设置-Git" class="headerlink" title="安装设置 Git"></a>安装设置 Git</h1><p><code>Git</code>是开源的分布式版本控制系统，用于敏捷高效地处理项目。网站在本地搭建好后，需要使用<code>Git</code>同步到<code>GitHub</code>存储库。直接搜索<code>Git</code>到官网下载，检测是否安装成功，安装完成后，打开命令行窗口输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git --version</span><br></pre></td></tr></table></figure>

<p>返回版本信息，说明安装成功。<br>设置<code>Git</code>，依次输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git config --global user.name &quot;这里填你的 GitHub 用户名&quot;</span><br><span class="line">git config --global user.email &quot;这里填你的 GitHub 注册邮箱&quot;</span><br></pre></td></tr></table></figure>

<p>输入下列命令，生成<code>ssh</code>密钥文件:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa -C &quot;这里填你的 GitHub 注册邮箱&quot;</span><br></pre></td></tr></table></figure>

<p>之后会连续输入三个回车键确认，生成的密钥文件一般在<code>用户目录/.ssh </code>文件夹下，例如我的在<code>C:/Users/ZYZ/.ssh</code>文件夹下。<br>找到<code>id_rsa.pub</code>文件，复制里面的密钥，打开<code>GitHub -&gt; Settings -&gt; SSH keys</code>页面，新建<code> new SSH Key</code>，将复制的密钥添加进去，在<code>Git Bash</code>中或者<code>cmd</code>命令行检测<code>GitHub</code>公钥设置是否成功，输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssh git@github.com</span><br></pre></td></tr></table></figure>

<p>由于<code>Git</code>有设置单个文件上传下载时的默认大小，我曾经遇到过，这里提前设置<code>Git</code>缓存值大小，输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git config --global https.postBuffer 5242880000</span><br></pre></td></tr></table></figure>

<p>这个设置的数值大约是<code>500M</code>。</p>
<hr>
<h1 id="安装-Node-js-和-npm"><a href="#安装-Node-js-和-npm" class="headerlink" title="安装 Node.js 和 npm"></a>安装 Node.js 和 npm</h1><p>直接搜索<code>Node.js</code>到官网下载<br>安装时勾选<code>Add to Path</code>,注意安装<code>Node.js</code>包含环境变量及<code>npm</code>的安装，安装后，检测<code>Node.js</code>是否安装成功，在命令行中输入:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">node -v</span><br></pre></td></tr></table></figure>

<p>返回版本信息，则成功，检测<code>npm</code>是否安装成功，在命令行中输入:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm -v</span><br></pre></td></tr></table></figure>

<p>返回版本信息，则成功。如果未成功，则输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install</span><br></pre></td></tr></table></figure>

<hr>
<h1 id="安装-Hexo-生成博客文件"><a href="#安装-Hexo-生成博客文件" class="headerlink" title="安装 Hexo,生成博客文件"></a>安装 Hexo,生成博客文件</h1><p><code>Hexo</code>就是我们的个人博客网站的框架， 这里需要自己选某个盘创建一个文件夹，可以命名为<code>Blog</code>，<code>Hexo</code>框架与以后自己要编辑发布的网页都在这个文件夹中。命令行进入到这个文件夹，<code>Windows</code>下切换盘符直接使用命令 <code>盘符:</code>，比如切换到 F 盘：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">F:</span><br></pre></td></tr></table></figure>

<p>切换文件夹命令<code>cd 文件夹名</code>,比如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cd Blog</span><br></pre></td></tr></table></figure>

<p>切换到上级目录使用命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cd ..</span><br></pre></td></tr></table></figure>

<p>命令行进入到<code>Blog</code>文件夹，使用<code>npm</code>安装<code>Hexo</code>，输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install -g hexo-cli</span><br></pre></td></tr></table></figure>

<p>安装时间较长耐心等待，安装完成后，初始化博客，输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo init</span><br></pre></td></tr></table></figure>

<p>我的流程中创建了<code>Blog</code>文件夹作为博客根目录，因此这里直接在博客根目录使用命令<code>hexo init</code>，如果想在其他文件夹下创建博客根目录可以使用命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo init 这里写想创建的文件夹的名字</span><br></pre></td></tr></table></figure>

<p>上面这条命令会创建一个文件夹作为博客根目录，与我上面的<code>Blog</code>目录一样，因此如果这样创建，之后的配置也要进入到这个文件夹配置。但是要注意，<code>hexo init</code>命令的目标必须是空文件夹。<br>接着生成博客文件，在博客根目录输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo g</span><br></pre></td></tr></table></figure>

<p>此时，根目录生成<code>public</code>目录最基本的博客文件已经生成，接下来预览博客，输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo s</span><br></pre></td></tr></table></figure>

<p>提示：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Hexo is running at http:&#x2F;&#x2F;localhost:4000 . Press Ctrl+C to stop.</span><br></pre></td></tr></table></figure>

<p>打开浏览器，输入网址<code>localhost:4000</code>，即可预览博客。</p>
<hr>
<h1 id="Hexo-命令介绍"><a href="#Hexo-命令介绍" class="headerlink" title="Hexo 命令介绍"></a>Hexo 命令介绍</h1><p>常用的<code>Hexo</code>命令</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm update hexo -g #升级</span><br><span class="line">hexo init #初始化博客</span><br><span class="line"></span><br><span class="line">hexo n &quot;博客文章名&quot; &#x3D;&#x3D; hexo new &quot;博客文章名&quot; #新建文章</span><br><span class="line">hexo g &#x3D;&#x3D; hexo generate #生成博客网页文件</span><br><span class="line">hexo s &#x3D;&#x3D; hexo server #启动服务预览</span><br><span class="line">hexo d &#x3D;&#x3D; hexo deploy #部署生成的博客文件到服务器</span><br><span class="line"></span><br><span class="line">hexo server #Hexo会监视文件变动并自动更新，无须重启服务器</span><br><span class="line">hexo server -s #静态模式</span><br><span class="line">hexo server -p 7000 #更改端口</span><br><span class="line">hexo server -i 192.168.1.1 #自定义 IP</span><br><span class="line">hexo clean #清除缓存</span><br><span class="line"></span><br><span class="line">hexo s --debug #以调试模式启动本地网站</span><br></pre></td></tr></table></figure>

<p>如果配置了博客文件，<code>Ctrl+F5</code>强制刷新后页面不见有变化，可以使用命令<code>hexo clean</code>清理已生成博客网页文件，之后使用<code>hexo g</code>命令重新生成。<br>更改配置文件时，常用命令<code>hexo s --debug</code>以调试模式启动本地网站,此模式下，对文件的更改无需停止网站只需刷新即可看到效果。</p>
<hr>
<h1 id="把静态网页部署到-GitHub"><a href="#把静态网页部署到-GitHub" class="headerlink" title="把静态网页部署到 GitHub"></a>把静态网页部署到 GitHub</h1><p>首先，创建在<code>Github</code>上创建一个存储库，名称格式必须是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">username.github.io</span><br></pre></td></tr></table></figure>

<p><code>username</code>是你注册<code>Github</code>时设置的用户名，存储库添加好后，到存储库<code>setting</code>页面往下翻可以看到<code>Github pages</code>项目，点进去会提示你，网页已经发布到:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">https:&#x2F;&#x2F;username.github.io&#x2F;</span><br></pre></td></tr></table></figure>

<p>如果没有<code>Github pages</code>，则是存储库名称格式有误，需要修改。<br>我们在第一步已经设置添加了密钥，在安装插件，设置博客配置文件后，就可以推送静态网页文件到<code>GitHub</code>存储库了。首先安装插件，输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>

<p>修改<code> 博客配置文件</code>即博客根目录下的<code>_config.yml </code>以下部分：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">deploy:</span><br><span class="line">  type: git</span><br><span class="line">  repo: https:&#x2F;&#x2F;github.com&#x2F;username&#x2F;username.github.io.git</span><br><span class="line">  branch: master</span><br></pre></td></tr></table></figure>

<p>在使用<code>hexo g</code>命令生成博客网页文件后，将文件推送到<code>Github</code>,输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo d</span><br></pre></td></tr></table></figure>

<p>到这里，你的静态网页已经部署到了<code>Github</code>上,到网址<code>https://username.github.io/</code>即可访问。<br>因为是静态网页，所以当你写好一篇文章，在本地调试好之后，需要再次使用命令<code>hexo d</code>即可更新网页。<br>此外，可以自己注册一个个性的域名，在<code>Github pages</code>与这个网页绑定，这个后面再写。</p>
<hr>
<h1 id="next-主题配置优化"><a href="#next-主题配置优化" class="headerlink" title="next 主题配置优化"></a>next 主题配置优化</h1><p><code>Hexo</code>有多种主题可以选择，我使用了<code>next</code>主题,这里写下基本的优化。</p>
<h2 id="下载-next-主题"><a href="#下载-next-主题" class="headerlink" title="下载 next 主题"></a>下载 next 主题</h2><p>主题在<code>Github</code>社区：<a href="https://github.com/theme-next/hexo-theme-next">https://github.com/theme-next/hexo-theme-next</a><br>在博客根目录输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone https:&#x2F;&#x2F;github.com&#x2F;theme-next&#x2F;hexo-theme-next themes&#x2F;next</span><br></pre></td></tr></table></figure>

<p>这是将<code>next</code>主题文件下载到了博客根目录下<code>themes</code>文件夹下的<code>next</code>文件夹，没有<code>next</code>文件夹的话，会自动新建，速度慢的话，使用镜像下载，以下命令二选一：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone https:&#x2F;&#x2F;github.com.cnpmjs.org&#x2F;theme-next&#x2F;hexo-theme-next themes&#x2F;next</span><br><span class="line">git clone https:&#x2F;&#x2F;git.sdut.me&#x2F;theme-next&#x2F;hexo-theme-next themes&#x2F;next</span><br></pre></td></tr></table></figure>

<p>使用<code>Git</code>下载东西慢时一般都可以将链接中的<code>github.com</code>替换为上面的镜像网站加速，后面的一些资源同样适用，还有一些别的镜像源，上面的失效的话，可以搜索引擎查找别的来代替。</p>
<h2 id="修改主题为-next，并进行基本配置"><a href="#修改主题为-next，并进行基本配置" class="headerlink" title="修改主题为 next，并进行基本配置"></a>修改主题为 next，并进行基本配置</h2><p>主题下载完之后，在你博客根目录下的<code>博客配置文件</code> <code> _config.yml</code>文件中，查找、修改<code>theme</code>值为主题名字<code>next</code>：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">theme: next</span><br></pre></td></tr></table></figure>

<p>设置<code>博客配置文件</code>，<code>title</code>为<code>博客名称</code>，<code>author</code>为<code>作者昵称</code>，<code>description</code>为<code>描述</code>，<code>subtitle</code>为<code>副标题</code>，<code>keywords</code>为<code>关键词</code>，<code>timezone</code>为<code>时区</code>，<code>language</code>为<code>界面语言</code>，可以按需求填写：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">title: Something</span><br><span class="line">subtitle: &quot;Hello!&quot;</span><br><span class="line">description: &quot;一直想变成程序猿的通信狗&quot;</span><br><span class="line">keywords:</span><br><span class="line">author: The ZYZ</span><br><span class="line">language: zh-CN</span><br><span class="line">timezone: &quot;&quot;</span><br></pre></td></tr></table></figure>

<p><code>next</code>自带四种主题风格，可以在<code>主题配置文件</code>搜索<code>Scheme</code>项修改，，将需要的风格前的<code>#</code>去掉，注意只能开启一个风格。这个<code>主题配置文件</code>和<code>博客配置文件</code>名称是一样的，但是位于下载的 next 主题目录下，修改配置时不要和<code>博客配置文件</code>弄混。<br>修改<code>主题配置文件</code>中<code>url</code>为<code>Github Page</code>给出的链接：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># URL</span><br><span class="line">## Set your site url here. For example, if you use GitHub Page, set url as &#39;https:&#x2F;&#x2F;username.github.io&#x2F;project&#39;</span><br><span class="line">url: https:&#x2F;&#x2F;username.github.io</span><br></pre></td></tr></table></figure>

<h2 id="设置博客头像"><a href="#设置博客头像" class="headerlink" title="设置博客头像"></a>设置博客头像</h2><p>编辑<code>主题配置文件</code>，搜索修改字段<code>avatar</code>中<code>url</code>值,设置为头像的链接，可以设置成在线的图片地址，也可以把图片放在本地：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">avatar:</span><br><span class="line">  # Replace the default image and set the url here.</span><br><span class="line">  url: &#x2F;images&#x2F;avatar.jpg</span><br><span class="line">  # If true, the avatar will be dispalyed in circle.</span><br><span class="line">  rounded: true</span><br><span class="line">  # If true, the avatar will be rotated with the cursor.</span><br><span class="line">  rotated: true</span><br></pre></td></tr></table></figure>

<p><code>rounded</code>设置是否圆形头像，<code>rotated</code>设置是否鼠标指向时旋转头像。</p>
<h2 id="设置网站图标"><a href="#设置网站图标" class="headerlink" title="设置网站图标"></a>设置网站图标</h2><p>在<a href="https://www.easyicon.net/">EasyIcon</a>中分别找一张<code>16*16</code>与<code>32*32</code>的<code>ico</code>图标,或者去别的网站下载或者自己制作，并将图标名称改为<code>favicon16.ico</code>与<code>favicon32.ico</code>，然后把图标放在<code>/themes/next/source/images</code>或者放在根目录的<code>/source/images</code>文件夹里。<br>在<code>主题配置文件</code>内搜索<code>favicon</code>字段，把<code>small</code>、<code>medium</code>字段的地址修改为<code>/images/favicon16.ico</code>与<code>/images/favicon32.ico</code>即可。</p>
<h2 id="设置赞赏功能"><a href="#设置赞赏功能" class="headerlink" title="设置赞赏功能"></a>设置赞赏功能</h2><p>主题自带<code>支付宝</code>跟<code>微信</code>及<code>比特币</code>打赏功能，只需要<code>主题配置文件</code>中配置启用，并设置<code>微信</code>和<code>支付宝</code>收款二维码图片地址即可开启功能。搜索<code>reward</code>字段，将<code>wechatpay</code>及<code>alipay</code>字段前面的<code>#</code>去掉即可。在<code>source</code>文件夹中放置<code>支付宝</code>跟<code>微信</code>收款码并修改文件名字即可。<code>comment</code>字段自带为打赏按钮上方的文字，不需要的话可以在前面加<code>#</code>注释掉。</p>
<h2 id="实现全站文章字数统计及阅读时长"><a href="#实现全站文章字数统计及阅读时长" class="headerlink" title="实现全站文章字数统计及阅读时长"></a>实现全站文章字数统计及阅读时长</h2><p>博客根目录输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install hexo-symbols-count-time --save</span><br></pre></td></tr></table></figure>

<p><code>博客配置文件</code>底部添加如下内容：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">symbols_count_time:</span><br><span class="line">  symbols: true</span><br><span class="line">  time: true</span><br><span class="line">  total_symbols: true</span><br><span class="line">  total_time: true</span><br></pre></td></tr></table></figure>

<h2 id="设置加载动画"><a href="#设置加载动画" class="headerlink" title="设置加载动画"></a>设置加载动画</h2><p>最新版本的<code>next</code>主题内置了加载条功能,博客根目录打开命令行，输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone https:&#x2F;&#x2F;git.sdut.me&#x2F;theme-next&#x2F;theme-next-pace themes&#x2F;next&#x2F;source&#x2F;lib&#x2F;pace</span><br></pre></td></tr></table></figure>

<p>然后<code>主题配置文件</code>搜索<code>pace</code>字段，设置<code>enable</code>值为<code>true</code>即可开启加载条功能，按照提示修改下方的<code>theme</code>字段即可修改加载条的样式。</p>
<h2 id="增加本地搜索功能"><a href="#增加本地搜索功能" class="headerlink" title="增加本地搜索功能"></a>增加本地搜索功能</h2><p>博客根目录打开命令行，输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-searchdb --save</span><br></pre></td></tr></table></figure>

<p>之后编辑<code>博客配置文件</code>，新增以下内容到任意位置：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">search:</span><br><span class="line">  path: search.xml</span><br><span class="line">  field: post</span><br><span class="line">  format: html</span><br><span class="line">  limit: 10000</span><br></pre></td></tr></table></figure>

<p><code>主题配置文件</code>搜索<code>local_search</code>字段，设置<code>enable</code>值为<code>true</code>即可。</p>
<h2 id="网页底部添加网站运行时间"><a href="#网页底部添加网站运行时间" class="headerlink" title="网页底部添加网站运行时间"></a>网页底部添加网站运行时间</h2><p>打开<code>/themes/next/layout/_partials/</code>文件夹下的<code>footer.swig</code>文件并添加下列代码,代码可以修改为你创建博客的时间：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div&gt;</span><br><span class="line">&lt;span id&#x3D;&quot;timeDate&quot;&gt;载入天数...&lt;&#x2F;span&gt;&lt;span id&#x3D;&quot;times&quot;&gt;载入时分秒...&lt;&#x2F;span&gt;</span><br><span class="line">&lt;script&gt;</span><br><span class="line">    var now &#x3D; new Date();</span><br><span class="line">    function createtime() &#123;</span><br><span class="line">        var grt&#x3D; new Date(&quot;03&#x2F;04&#x2F;2020 00:00:00&quot;);</span><br><span class="line">        now.setTime(now.getTime()+250);</span><br><span class="line">        days &#x3D; (now - grt ) &#x2F; 1000 &#x2F; 60 &#x2F; 60 &#x2F; 24; dnum &#x3D; Math.floor(days);</span><br><span class="line">        hours &#x3D; (now - grt ) &#x2F; 1000 &#x2F; 60 &#x2F; 60 - (24 * dnum); hnum &#x3D; Math.floor(hours);</span><br><span class="line">        if(String(hnum).length &#x3D;&#x3D;1 )&#123;hnum &#x3D; &quot;0&quot; + hnum;&#125; minutes &#x3D; (now - grt ) &#x2F; 1000 &#x2F;60 - (24 * 60 * dnum) - (60 * hnum);</span><br><span class="line">        mnum &#x3D; Math.floor(minutes); if(String(mnum).length &#x3D;&#x3D;1 )&#123;mnum &#x3D; &quot;0&quot; + mnum;&#125;</span><br><span class="line">        seconds &#x3D; (now - grt ) &#x2F; 1000 - (24 * 60 * 60 * dnum) - (60 * 60 * hnum) - (60 * mnum);</span><br><span class="line">        snum &#x3D; Math.round(seconds); if(String(snum).length &#x3D;&#x3D;1 )&#123;snum &#x3D; &quot;0&quot; + snum;&#125;</span><br><span class="line">        document.getElementById(&quot;timeDate&quot;).innerHTML &#x3D; &quot;本站已安全运行 &quot;+dnum+&quot; 天 &quot;;</span><br><span class="line">        document.getElementById(&quot;times&quot;).innerHTML &#x3D; hnum + &quot; 小时 &quot; + mnum + &quot; 分 &quot; + snum + &quot; 秒&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">setInterval(&quot;createtime()&quot;,250);</span><br><span class="line">&lt;&#x2F;script&gt;</span><br><span class="line">&lt;&#x2F;div&gt;</span><br></pre></td></tr></table></figure>

<h2 id="增加烟花爆炸效果"><a href="#增加烟花爆炸效果" class="headerlink" title="增加烟花爆炸效果"></a>增加烟花爆炸效果</h2><p>在路径<code>themes/next/source/js/src/</code>下创建文件<code>fireworks.js</code>，添加以下代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;use strict&quot;;</span><br><span class="line">function updateCoords(e)&#123;</span><br><span class="line">    pointerX&#x3D;(e.clientX||e.touches[0].clientX)-canvasEl.getBoundingClientRect().left,</span><br><span class="line">    pointerY&#x3D;e.clientY||e.touches[0].clientY-canvasEl.getBoundingClientRect().top</span><br><span class="line">&#125;</span><br><span class="line">function setParticuleDirection(e)&#123;</span><br><span class="line">    var t&#x3D;anime.random(0,360)*Math.PI&#x2F;180,</span><br><span class="line">    a&#x3D;anime.random(15,60),&#x2F;&#x2F;爆炸范围(50,180)</span><br><span class="line">    n&#x3D;[-1,1][anime.random(0,1)]*a;</span><br><span class="line">    return&#123;x:e.x+n*Math.cos(t),y:e.y+n*Math.sin(t)&#125;</span><br><span class="line">&#125;</span><br><span class="line">function createParticule(e,t)&#123;</span><br><span class="line">    var a&#x3D;&#123;&#125;;</span><br><span class="line">    return a.x&#x3D;e,</span><br><span class="line">    a.y&#x3D;t,</span><br><span class="line">    a.color&#x3D;colors[anime.random(0,colors.length-1)],</span><br><span class="line">    a.radius&#x3D;anime.random(5,15),&#x2F;&#x2F;(16,32)</span><br><span class="line">    a.endPos&#x3D;setParticuleDirection(a),</span><br><span class="line">    a.draw&#x3D;function()&#123;</span><br><span class="line">        ctx.beginPath(),</span><br><span class="line">        ctx.arc(a.x,a.y,a.radius,0,2*Math.PI,!0),</span><br><span class="line">        ctx.fillStyle&#x3D;a.color,ctx.fill()</span><br><span class="line">    &#125;,a</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">function createCircle(e,t)&#123;</span><br><span class="line">    var a&#x3D;&#123;&#125;;</span><br><span class="line">    return a.x&#x3D;e,</span><br><span class="line">    a.y&#x3D;t,</span><br><span class="line">    a.color&#x3D;&quot;#F00&quot;,</span><br><span class="line">    a.radius&#x3D;0.1,</span><br><span class="line">    a.alpha&#x3D;0.5,</span><br><span class="line">    a.lineWidth&#x3D;1,&#x2F;&#x2F;6</span><br><span class="line">    a.draw&#x3D;function()&#123;</span><br><span class="line">        ctx.globalAlpha&#x3D;a.alpha,</span><br><span class="line">        ctx.beginPath(),</span><br><span class="line">        ctx.arc(a.x,a.y,a.radius,0,2*Math.PI,!0),</span><br><span class="line">        ctx.lineWidth&#x3D;a.lineWidth,</span><br><span class="line">        ctx.strokeStyle&#x3D;a.color,</span><br><span class="line">        ctx.stroke(),</span><br><span class="line">        ctx.globalAlpha&#x3D;1&#125;,a</span><br><span class="line">    &#125;</span><br><span class="line">function renderParticule(e)&#123;</span><br><span class="line">    for(var t&#x3D;0;t&lt;e.animatables.length;t++)</span><br><span class="line">    &#123;e.animatables[t].target.draw()&#125;</span><br><span class="line">&#125;</span><br><span class="line">function animateParticules(e,t)&#123;</span><br><span class="line">    for(var a&#x3D;createCircle(e,t),n&#x3D;[],i&#x3D;0;i&lt;numberOfParticules;i++)</span><br><span class="line">    &#123;n.push(createParticule(e,t))&#125;</span><br><span class="line">    anime.timeline().add(&#123;targets:n,x:function(e)&#123;return e.endPos.x&#125;,</span><br><span class="line">    y:function(e)&#123;return e.endPos.y&#125;,radius:0.1,duration:anime.random(1200,1800),easing:&quot;easeOutExpo&quot;,</span><br><span class="line">    update:renderParticule&#125;).add(&#123;targets:a,radius:anime.random(80,160),lineWidth:0,alpha:&#123;value:0,</span><br><span class="line">        easing:&quot;linear&quot;,duration:anime.random(600,800)&#125;,duration:anime.random(1200,1800),easing:&quot;easeOutExpo&quot;,</span><br><span class="line">        update:renderParticule,offset:0&#125;)&#125;</span><br><span class="line">function debounce(e,t)&#123;</span><br><span class="line">    var a;</span><br><span class="line">    return function()&#123;</span><br><span class="line">        var n&#x3D;this,i&#x3D;arguments;</span><br><span class="line">        clearTimeout(a),a&#x3D;setTimeout(function()&#123;e.apply(n,i)&#125;,t)</span><br><span class="line">    &#125;</span><br><span class="line">    &#125;</span><br><span class="line">var canvasEl&#x3D;document.querySelector(&quot;.fireworks&quot;);</span><br><span class="line">if(canvasEl)</span><br><span class="line">&#123;var ctx&#x3D;canvasEl.getContext(&quot;2d&quot;),</span><br><span class="line">    numberOfParticules&#x3D;8,&#x2F;&#x2F;数目</span><br><span class="line">    pointerX&#x3D;0,</span><br><span class="line">    pointerY&#x3D;0,</span><br><span class="line">    tap&#x3D;&quot;mousedown&quot;,</span><br><span class="line">    colors&#x3D;[&quot;#FF1461&quot;,&quot;#18FF92&quot;,&quot;#5A87FF&quot;,&quot;#FBF38C&quot;],</span><br><span class="line">    setCanvasSize&#x3D;debounce(function()&#123;canvasEl.width&#x3D;2*window.innerWidth,</span><br><span class="line">        canvasEl.height&#x3D;2*window.innerHeight,</span><br><span class="line">        canvasEl.style.width&#x3D;window.innerWidth+&quot;px&quot;,</span><br><span class="line">        canvasEl.style.height&#x3D;window.innerHeight+&quot;px&quot;,</span><br><span class="line">        canvasEl.getContext(&quot;2d&quot;).scale(2,2)&#125;,100),</span><br><span class="line">        render&#x3D;anime(&#123;duration:1&#x2F;0,update:function()&#123;ctx.clearRect(0,0,canvasEl.width,canvasEl.height)&#125;&#125;);</span><br><span class="line">        document.addEventListener(tap,function(e)&#123;&quot;sidebar&quot;!&#x3D;&#x3D;e.target.id&amp;&amp;&quot;toggle-sidebar&quot;!&#x3D;&#x3D;e.target.id&amp;&amp;&quot;A&quot;!&#x3D;&#x3D;e.target.nodeName&amp;&amp;&quot;IMG&quot;!&#x3D;&#x3D;e.target.nodeName&amp;&amp;(render.play(),updateCoords(e),animateParticules(pointerX,pointerY))&#125;,!1),</span><br><span class="line">        setCanvasSize(),window.addEventListener(&quot;resize&quot;,setCanvasSize,!1)&#125;</span><br></pre></td></tr></table></figure>

<p>修改<code>themes/next/layout/</code>路径下<code>_layout.swig</code>文件，在最下面<code>&lt;/body&gt;</code>行的上面添加以下代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;% if theme.fireworks %&#125;</span><br><span class="line">   &lt;canvas class&#x3D;&quot;fireworks&quot; style&#x3D;&quot;position: fixed;left: 0;top: 0;z-index: 1; pointer-events: none;&quot; &gt;&lt;&#x2F;canvas&gt;</span><br><span class="line">   &lt;script type&#x3D;&quot;text&#x2F;javascript&quot; src&#x3D;&quot;&#x2F;&#x2F;cdn.bootcss.com&#x2F;animejs&#x2F;2.2.0&#x2F;anime.min.js&quot;&gt;&lt;&#x2F;script&gt;</span><br><span class="line">   &lt;script type&#x3D;&quot;text&#x2F;javascript&quot; src&#x3D;&quot;&#x2F;js&#x2F;src&#x2F;fireworks.js&quot;&gt;&lt;&#x2F;script&gt;</span><br><span class="line">&#123;% endif %&#125;</span><br></pre></td></tr></table></figure>

<p>修改<code>主题配置文件</code>，最后面添加以下代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Fireworks</span><br><span class="line">fireworks: true</span><br></pre></td></tr></table></figure>

<h2 id="404-页面创建"><a href="#404-页面创建" class="headerlink" title="404 页面创建"></a>404 页面创建</h2><p>博客根目录下创建<code>404</code>页面，输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo new page 404</span><br></pre></td></tr></table></figure>

<p>在博客根目录下的<code>source/404/</code>路径下的<code>index.md</code>文件中,添加以下代码:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 404</span><br><span class="line">date: 2020-05-28 17:37:33</span><br><span class="line">---</span><br><span class="line">&lt;!DOCTYPE HTML&gt;</span><br><span class="line">  &lt;html&gt;</span><br><span class="line">  &lt;head&gt;</span><br><span class="line">    &lt;meta http-equiv&#x3D;&quot;content-type&quot; content&#x3D;&quot;text&#x2F;html;charset&#x3D;utf-8;&quot;&#x2F;&gt;</span><br><span class="line">    &lt;meta http-equiv&#x3D;&quot;X-UA-Compatible&quot; content&#x3D;&quot;IE&#x3D;edge,chrome&#x3D;1&quot; &#x2F;&gt;</span><br><span class="line">    &lt;meta name&#x3D;&quot;robots&quot; content&#x3D;&quot;all&quot; &#x2F;&gt;</span><br><span class="line">    &lt;meta name&#x3D;&quot;robots&quot; content&#x3D;&quot;index,follow&quot;&#x2F;&gt;</span><br><span class="line">    &lt;link rel&#x3D;&quot;stylesheet&quot; type&#x3D;&quot;text&#x2F;css&quot; href&#x3D;&quot;https:&#x2F;&#x2F;qzone.qq.com&#x2F;gy&#x2F;404&#x2F;style&#x2F;404style.css&quot;&gt;</span><br><span class="line">  &lt;&#x2F;head&gt;</span><br><span class="line">  &lt;body&gt;</span><br><span class="line">    &lt;script type&#x3D;&quot;text&#x2F;plain&quot; src&#x3D;&quot;http:&#x2F;&#x2F;www.qq.com&#x2F;404&#x2F;search_children.js&quot;</span><br><span class="line">            charset&#x3D;&quot;utf-8&quot; homePageUrl&#x3D;&quot;https:&#x2F;&#x2F;username.github.io&quot;  #填自己的博客地址</span><br><span class="line">            homePageName&#x3D;&quot;回到我的主页&quot;&gt;</span><br><span class="line">    &lt;&#x2F;script&gt;</span><br><span class="line">    &lt;script src&#x3D;&quot;https:&#x2F;&#x2F;qzone.qq.com&#x2F;gy&#x2F;404&#x2F;data.js&quot; charset&#x3D;&quot;utf-8&quot;&gt;&lt;&#x2F;script&gt;</span><br><span class="line">    &lt;script src&#x3D;&quot;https:&#x2F;&#x2F;qzone.qq.com&#x2F;gy&#x2F;404&#x2F;page.js&quot; charset&#x3D;&quot;utf-8&quot;&gt;&lt;&#x2F;script&gt;</span><br><span class="line">  &lt;&#x2F;body&gt;</span><br><span class="line">  &lt;&#x2F;html&gt;</span><br></pre></td></tr></table></figure>

<p>修改<code>themes/next/languages/</code>路径下<code>zh-CN.yml</code>文件中名字，<code>公益404</code> 修改为<code>公益</code>，再修改<code>主题配置文件</code>，开启<code>menu</code>模块的<code>commonweal</code>标签即可。</p>
<h2 id="添加-README-md-文件"><a href="#添加-README-md-文件" class="headerlink" title="添加 README.md 文件"></a>添加 README.md 文件</h2><p>每个项目下一般都有一个<code>README.md</code>文件，但是使用<code>hexo d</code>部署到仓库后，项目下是没有<code>README.md</code>文件的。在<code>Hexo</code>目录下的<code>source</code>目录下添加一个<code>README.md</code>文件，修改<code>博客配置文件</code>，将<code>skip_render</code>参数的值设置为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">skip_render: README.md</span><br></pre></td></tr></table></figure>

<p>再次使用<code>hexo d</code>命令部署博客的时候就不会在渲染<code>README.md</code>这个文件。</p>
<h2 id="添加网易云音乐"><a href="#添加网易云音乐" class="headerlink" title="添加网易云音乐"></a>添加网易云音乐</h2><p>打开网易云音乐网页版,到歌曲播放界面，生成外链播放器，复制 HTML 代码，<br>在<code>themes/next/layout/_macro/</code>路径下<code>sidebar.swig</code>文件中添加复制的代码。添加到不同位置，播放器的位置也不同，可以自行试验一下，注意不要破坏其他代码段，文件中最后四行代码不能分开。</p>
<h2 id="添加动态背景"><a href="#添加动态背景" class="headerlink" title="添加动态背景"></a>添加动态背景</h2><p>在博客根目录打开命令行，切换到<code>next</code>主题文件夹，输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone https:&#x2F;&#x2F;github.com&#x2F;theme-next&#x2F;theme-next-canvas-nest source&#x2F;lib&#x2F;canvas-nest</span><br></pre></td></tr></table></figure>

<p>修改<code>主题配置文件</code>,修改下列代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Canvas-nest</span><br><span class="line"># Dependencies: https:&#x2F;&#x2F;github.com&#x2F;theme-next&#x2F;theme-next-canvas-nest</span><br><span class="line">canvas_nest:</span><br><span class="line">  enable: true</span><br><span class="line">  onmobile: true # 是否在手机上显示</span><br><span class="line">  color: &quot;255,51,51&quot; # RGB颜色设置</span><br><span class="line">  opacity: 0.5 # 线条透明度</span><br><span class="line">  zIndex: -1 # 显示级别</span><br><span class="line">  count: 160 # 线条的数量，越多越卡</span><br></pre></td></tr></table></figure>

<p>如果想使用<code>CDN</code>加速的话需要设置：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vendors:</span><br><span class="line">  ...</span><br><span class="line">  canvas_nest: &#x2F;&#x2F;cdn.jsdelivr.net&#x2F;gh&#x2F;theme-next&#x2F;theme-next-canvas-nest@1.0.0&#x2F;canvas-nest.min.js</span><br><span class="line">  canvas_nest_nomobile: &#x2F;&#x2F;cdn.jsdelivr.net&#x2F;gh&#x2F;theme-next&#x2F;theme-next-canvas-nest@1.0</span><br></pre></td></tr></table></figure>

<h2 id="添加文章目录功能"><a href="#添加文章目录功能" class="headerlink" title="添加文章目录功能"></a>添加文章目录功能</h2><p>我们用<code>markdown</code>写东西的时候会有一级标题，二级标题，文章目录就是由这些构成的。一种方法在博客根目录下<code>source/_data</code>新建一个<code>styles.styl</code>文件。然后查找<code>主题配置文件</code>下的<code>custom_file_path</code>字段：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">custom_file_path:</span><br><span class="line">    style: source&#x2F;_data&#x2F;styles.styl</span><br></pre></td></tr></table></figure>

<p>将<code>style</code>前的注释取消掉即可。然后在<code>styles.styl</code>添加代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F;文章目录默认展开</span><br><span class="line">.post-toc .nav .nav-child &#123; display: block; &#125;</span><br><span class="line">.post-toc ol &#123;</span><br><span class="line">  font-size : 13px;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>接下来初始化文章目录，修改<code>主题配置文件</code>中<code>Table of Contents in the Sidebar</code>部分：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Table of Contents in the Sidebar</span><br><span class="line"># Front-matter variable (unsupport wrap expand_all).</span><br><span class="line">toc:</span><br><span class="line">  enable: true</span><br><span class="line">  # Automatically add list number to toc.</span><br><span class="line">  number: true</span><br><span class="line">  # If true, all words will placed on next lines if header width longer then sidebar width.</span><br><span class="line">  wrap: true</span><br><span class="line">  # If true, all level of TOC in a post will be displayed, rather than the activated part of it.</span><br><span class="line">  expand_all: true</span><br><span class="line">  # Maximum heading depth of generated toc.</span><br><span class="line">  max_depth: 6</span><br></pre></td></tr></table></figure>

<h2 id="网站背景"><a href="#网站背景" class="headerlink" title="网站背景"></a>网站背景</h2><p>关于<code>主题配置</code>有很多说的都是那个<code>_custom</code>文件。但是这个<code>next</code>主题是没有这个文件的。所以我们还在<code>style.styl</code>文件中配置。配置内容如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">body &#123;</span><br><span class="line">    background:url(http:&#x2F;&#x2F;***********&#x2F;******&#x2F;images&#x2F;bg.jpg);&#x2F;&#x2F;图片路径</span><br><span class="line">    background-repeat: no-repeat;&#x2F;&#x2F;是否重复平铺</span><br><span class="line">    background-attachment:fixed;&#x2F;&#x2F;是否随着网页上下滚动而滚动，fixed为固定</span><br><span class="line">    background-position:50% 50%;&#x2F;&#x2F;图片位置</span><br><span class="line">    background-size: cover;&#x2F;&#x2F;图片展示大小</span><br><span class="line">    -webkit-background-size: cover;</span><br><span class="line">    -o-background-size: cover;</span><br><span class="line">    -moz-background-size: cover;</span><br><span class="line">    -ms-background-size: cover;</span><br><span class="line">    opacity: 0.6;&#x2F;&#x2F;设置整个页面的透明度。该透明度如果不加在这里会出现bug，bug就是菜单栏无法点击。</span><br><span class="line">     #footer &gt; div &gt; div &#123;</span><br><span class="line">        color:#000000;&#x2F;&#x2F;底部文字颜色</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">.header-inner &#123;</span><br><span class="line">    background: #fff;&#x2F;&#x2F;设置侧边栏透明</span><br><span class="line">    opacity: 1;&#x2F;&#x2F;这里设置为1，否则搜索栏会消失</span><br><span class="line">&#125;</span><br><span class="line">.main-inner &#123;</span><br><span class="line">  background: #fff;</span><br><span class="line">  opacity: 1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>图片的<code>url</code>替换为你的<code>博客网址+images</code>即可。</p>
<h2 id="静态资源压缩"><a href="#静态资源压缩" class="headerlink" title="静态资源压缩"></a>静态资源压缩</h2><p>在博客根目录下，输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install gulp -g</span><br></pre></td></tr></table></figure>

<p>在依次安装<code>gulp</code>插件：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install gulp-minify-css --save</span><br><span class="line">npm install gulp-uglify --save</span><br><span class="line">npm install gulp-htmlmin --save</span><br><span class="line">npm install gulp-htmlclean --save</span><br><span class="line">npm install gulp-imagemin --save</span><br></pre></td></tr></table></figure>

<p>在博客根目录下添加<code>gulpfile.js</code>文件，文件内容如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var gulp &#x3D; require(&#39;gulp&#39;);</span><br><span class="line">var minifycss &#x3D; require(&#39;gulp-minify-css&#39;);</span><br><span class="line">var uglify &#x3D; require(&#39;gulp-uglify&#39;);</span><br><span class="line">var htmlmin &#x3D; require(&#39;gulp-htmlmin&#39;);</span><br><span class="line">var htmlclean &#x3D; require(&#39;gulp-htmlclean&#39;);</span><br><span class="line">var imagemin &#x3D; require(&#39;gulp-imagemin&#39;);</span><br><span class="line">&#x2F;&#x2F; 压缩css文件</span><br><span class="line">gulp.task(&#39;minify-css&#39;, function() &#123;</span><br><span class="line">  return gulp.src(&#39;.&#x2F;public&#x2F;**&#x2F;*.css&#39;)</span><br><span class="line">  .pipe(minifycss())</span><br><span class="line">  .pipe(gulp.dest(&#39;.&#x2F;public&#39;));</span><br><span class="line">&#125;);</span><br><span class="line">&#x2F;&#x2F; 压缩html文件</span><br><span class="line">gulp.task(&#39;minify-html&#39;, function() &#123;</span><br><span class="line">  return gulp.src(&#39;.&#x2F;public&#x2F;**&#x2F;*.html&#39;)</span><br><span class="line">  .pipe(htmlclean())</span><br><span class="line">  .pipe(htmlmin(&#123;</span><br><span class="line">    removeComments: true,</span><br><span class="line">    minifyJS: true,</span><br><span class="line">    minifyCSS: true,</span><br><span class="line">    minifyURLs: true,</span><br><span class="line">  &#125;))</span><br><span class="line">  .pipe(gulp.dest(&#39;.&#x2F;public&#39;))</span><br><span class="line">&#125;);</span><br><span class="line">&#x2F;&#x2F; 压缩js文件</span><br><span class="line">gulp.task(&#39;minify-js&#39;, function() &#123;</span><br><span class="line">    return gulp.src([&#39;.&#x2F;public&#x2F;**&#x2F;.js&#39;,&#39;!.&#x2F;public&#x2F;js&#x2F;**&#x2F;*min.js&#39;])</span><br><span class="line">        .pipe(uglify())</span><br><span class="line">        .pipe(gulp.dest(&#39;.&#x2F;public&#39;));</span><br><span class="line">&#125;);</span><br><span class="line">&#x2F;&#x2F; 压缩 public&#x2F;demo 目录内图片</span><br><span class="line">gulp.task(&#39;minify-images&#39;, function() &#123;</span><br><span class="line">    gulp.src(&#39;.&#x2F;public&#x2F;demo&#x2F;**&#x2F;*.*&#39;)</span><br><span class="line">        .pipe(imagemin(&#123;</span><br><span class="line">           optimizationLevel: 5, &#x2F;&#x2F;类型：Number  默认：3  取值范围：0-7（优化等级）</span><br><span class="line">           progressive: true, &#x2F;&#x2F;类型：Boolean 默认：false 无损压缩jpg图片</span><br><span class="line">           interlaced: false, &#x2F;&#x2F;类型：Boolean 默认：false 隔行扫描gif进行渲染</span><br><span class="line">           multipass: false, &#x2F;&#x2F;类型：Boolean 默认：false 多次优化svg直到完全优化</span><br><span class="line">        &#125;))</span><br><span class="line">        .pipe(gulp.dest(&#39;.&#x2F;public&#x2F;uploads&#39;));</span><br><span class="line">&#125;);</span><br><span class="line">&#x2F;&#x2F; 默认任务</span><br><span class="line">gulp.task(&#39;default&#39;, [</span><br><span class="line">  &#39;minify-html&#39;,&#39;minify-css&#39;,&#39;minify-js&#39;,&#39;minify-images&#39;</span><br><span class="line">]);</span><br></pre></td></tr></table></figure>

<p>只需要每次在执行<code>hexo g</code>命令后执行<code>gulp</code>命令,就可以实现对静态资源的压缩，压缩完成后再执行<code>hexo d</code>命令部署到<code>Github</code>。<br>这里如果使用<code>gulp</code>命令提示缺少<code>gulp</code>，可以执行命令再次安装：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm install gulp</span><br></pre></td></tr></table></figure>

<h2 id="写博客文章"><a href="#写博客文章" class="headerlink" title="写博客文章"></a>写博客文章</h2><p>在你的博客根目录下输入命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo new article</span><br></pre></td></tr></table></figure>

<p>这里的<code>article</code>是你的文章的名称,输入这样的命令之后<code>source/_posts</code>路径下就会生成一个<code>article.md</code>文件，在这个文件下就可以写上博客内容了,是用<code>Markdown</code>的语法去写。<code>Markdown</code>的语法很简单，如果之前没用过可以学一下<code>Markdown</code>语法说明，一般一个小时左右就能掌握。</p>
<h2 id="创建标签和分类"><a href="#创建标签和分类" class="headerlink" title="创建标签和分类"></a>创建标签和分类</h2><p>创建标签和分类要确定你的<code>博客配置文件</code>是否有打开了<code>tag_dir: tags </code>和<code>category_dir: categories</code>。<br><code>hexo</code>打开标签功能：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo new page tags</span><br></pre></td></tr></table></figure>

<p>这样，博客根目录的<code>source/</code>下生成<code>tags/</code>并生成<code>index.md</code>文件，修改部分内容：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">type: &quot;tags&quot;</span><br><span class="line">comments: false</span><br></pre></td></tr></table></figure>

<p>要为文章设置标签就可以在文章的头部写上：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">tags:</span><br><span class="line"> - Tag1</span><br><span class="line"> - Tag2</span><br><span class="line"> - Tag3</span><br></pre></td></tr></table></figure>

<p>比如我现在的这篇文章标签是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">tags:</span><br><span class="line">  - Hexo</span><br><span class="line">  - Github</span><br><span class="line">  - next主题</span><br><span class="line">  - 主题优化</span><br></pre></td></tr></table></figure>

<p>打开<code>hexo</code>分类功能：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo new page categories</span><br></pre></td></tr></table></figure>

<p>同样的，博客根目录的<code>source/</code>下生成<code>categories/</code>并生成<code>index.md</code>文件，修改部分内容：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">type: &quot;categories&quot;</span><br><span class="line">comments: false</span><br></pre></td></tr></table></figure>

<p>要为文章设置分类就可以在文章的头部写上：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">categories:</span><br><span class="line"> - 分类1</span><br><span class="line"> - 分类2</span><br></pre></td></tr></table></figure>

<p>比如这篇文章的分类是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">categories:</span><br><span class="line">  - 个人网站</span><br><span class="line">  - 教程</span><br></pre></td></tr></table></figure>

<h2 id="文章加密访问"><a href="#文章加密访问" class="headerlink" title="文章加密访问"></a>文章加密访问</h2><p>修改<code>themes/next/layout/_partials/</code>路径下的<code>head.swig</code>文件,在<code>&#123;% if theme.pace %&#125;`标签下的`&#123;% endif %&#125;</code>之前插入代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">  &lt;script&gt;</span><br><span class="line">    (function()&#123;</span><br><span class="line">        if(&#39;&#123;&#123; page.password &#125;&#125;&#39;)&#123;</span><br><span class="line">            if (prompt(&#39;请输入文章密码&#39;) !&#x3D;&#x3D; &#39;&#123;&#123; page.password &#125;&#125;&#39;)&#123;</span><br><span class="line">                alert(&#39;密码错误&#39;);</span><br><span class="line">                history.back();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;)();</span><br><span class="line">&lt;&#x2F;script&gt;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>在文章上应用：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">title: 个人收藏网页</span><br><span class="line">date: 2021-04-20 13:00:00</span><br><span class="line">type:</span><br><span class="line">top:</span><br><span class="line">comments:</span><br><span class="line">categories: -密码分享</span><br><span class="line">tags: -密码分享</span><br><span class="line">password: 123456</span><br><span class="line">---</span><br></pre></td></tr></table></figure>

<h2 id="给文章添加评论功能"><a href="#给文章添加评论功能" class="headerlink" title="给文章添加评论功能"></a>给文章添加评论功能</h2><p>评论是博客交互的重要方式之一，当别人看了你的文章，有问题想跟你探讨，评论是最简单交流方式。<br>在很多<code>hexo</code>主题都内置了第三方评论的系统，较常见的有：<br><a href="http://duoshuo.com/">多说</a><br><a href="https://changyan.kuaizhan.com/">畅言</a><br><a href="https://github.com/xcss/Valine">Valine</a><br><a href="https://disqus.com/">Disqus</a><br><a href="http://www.uyan.cc/">友言</a><br><a href="https://github.com/imsun/gitment">gitment</a><br>一般注册平台，创建应用后，平台会给你一个<code>appid</code>和<code>appkey</code>。把这些信息填写到<code>主题配置文件</code>中即可。以我注册了<code>畅言平台</code>为例，查找<code>主题配置文件</code>中的<code>changyan</code>字段，按说明设置即可：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">changyan:</span><br><span class="line">  enable: true</span><br><span class="line">  appid: 这里写你在畅言的appid</span><br><span class="line">  appkey: 这里写上你在畅言的appkey</span><br></pre></td></tr></table></figure>

<p>设置好之后，<code>hexo clean</code>并重新<code>hexo g</code>生成一下就可以了,所有新建的页面都将自动开启评论。<br>如果要关闭新建页面的评论功能,请在页面的<code>Front-matter</code>里添加<code>comments</code>字段，并将值设置为<code>false</code>,如下所示：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: All tags</span><br><span class="line">date: 2021-04-20 16:00:00</span><br><span class="line">type: &quot;tags&quot;</span><br><span class="line">comments: false</span><br><span class="line">---</span><br></pre></td></tr></table></figure>

<h2 id="图床"><a href="#图床" class="headerlink" title="图床"></a>图床</h2><p>当博文中有少量图片是，可以直接把图片存放在<code>source</code>文件夹中，但图片会占据大量的存储的空间，加载相对缓慢，我们可以考虑把文章里的图片上传到某个网站，然后获得外部链接，写文章时使用<code>Markdown</code>语法，<code>![图片信息](外部链接)</code>, 完成图片的插入，这种网站就被称为图床。建议使用比较知名的，比如：<code>腾讯云</code>、<code>网易蜂巢</code>、<code>七牛云</code>、<code>京东云</code>等。</p>
<h2 id="参考文章"><a href="#参考文章" class="headerlink" title="参考文章"></a>参考文章</h2><p><a href="https://lixint.github.io/hexo-theme-diy.html">Hexo 博客主题安装及 Next 主题个性化修改</a><br><a href="https://blog.csdn.net/qq_44219883/article/details/105178206?spm=1001.2014.3001.5501">Hexo+next 主题配置踩的坑</a><br><a href="https://siriusq.top/">Sirius</a><br><a href="https://zhuanlan.zhihu.com/p/26625249">GitHub+Hexo 搭建个人网站详细教程 - 知乎</a></p>
<hr>
]]></content>
      <categories>
        <category>个人网站</category>
        <category>教程</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
        <tag>Github</tag>
        <tag>next主题</tag>
        <tag>主题优化</tag>
      </tags>
  </entry>
</search>
